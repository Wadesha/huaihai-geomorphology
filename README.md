# 淮海地貌现场手册

一份可以直接带去实地的淮海地貌指南：把淮海常见的地貌现象按 **9 类营力** 归纳为「原理层 → 实例层 → 判定层」，
实例全部落在**淮海地区现存、可到达的具体地点**（苏北 · 皖北 · 鲁南 · 豫东，淮海经济区核心）。

同系列另有全国版：[../geomorphology-field-guide](../geomorphology-field-guide)。

## 版式原则（重要）

**单页标签式：顶排一排短名卡片（总览 / 原理 / 实例 / 判定 / 时间轴 / 来源），点哪张出现哪块内容；
「实例」下还有第二排 16 处地点的短名卡片，点哪张出现哪一处的完整内容。**
正文仍以连续散文（大段文字）呈现，不用表格、统计块、步骤条、标签块、侧栏与列表；
导航卡片、标题层级与来源链接保留。数字仍逐条标注来源，冲突口径并列不合并。
`validate.py` 会自动检查散文约束与卡片结构，出现 `STRUCTURED` 命中即视为不合格。

正文段落统一首行缩进，阅读栏宽 820 px；明暗两套主题跟随系统并可手动切换。
路由基于 hash：`#home / #prins / #cases / #field / #time / #srcs`，
实例深链为 `#c/<slug>`（如 `#c/daigu`）。

## 站点结构

全部内容在 `docs/index.html` 一个文件里，按六个视图组织：

总览视图讲清三层用法、地域格局（按苏北、皖北、鲁南、豫东四片列出全部实例）以及 9 类营力各自控制什么变量、留下什么产物、对应淮海哪些实例。

原理视图先给四条底层框架——内力与外力、成因与形态与年代、规模等级与地域分带、时间是隐藏变量——再逐类展开 9 类营力系统，最后讲怎么用这套框架读一处具体地方。

实例视图是核心：一排 16 张地点短名卡片 + 各自的完整内容。单个实例固定包含七块内容：名称与一句话立意、含位置与可达性的导语、现场能看到什么、背后的原理（成因机制链）、这些数字在说什么、实测数据、争议与口径、来源。

判定视图摊开 6 组淮海高发的易混淆地貌，给出通用观察顺序、测量与记录的最小工具集，以及这套方法的边界。

时间轴视图把 13 个时间锚点按先后排开，每个锚点后接一句按营力分别措辞的短评，并说明年代口径的处理方式。

来源视图把 44 条来源按实例分列，并交代数据口径与使用边界。

## 16 个实例（按分区）

苏北 6 处：洪泽湖（一条河把一群小湖挤成悬在平原上的大湖）、废黄河（黄河故道，一条死去的河成了两大水系的分水岭）、盐城黄海湿地与辐射沙洲、徐州贾汪潘安湖（采煤塌陷地变成的湖）、骆马湖（断裂带里陷下去的湖盆）、连云港云台山（江苏最高峰，断块山）。

皖北 2 处：宿州皇藏峪（皖北平原上的石灰岩岛状残丘）、淮北相山（被采石「啃」过的残丘）。

鲁南 5 处：蒙阴岱崮地貌（中国第五大造型地貌）、郯城—郯庐断裂带与 1668 年大地震、枣庄熊耳山与抱犊崮、临郯苍平原（沂沭河冲积平原）、南四湖（微山湖）。

豫东 3 处：兰考—民权黄泛风沙地、商丘黄河故道与天沐湖、永城芒砀山。

## 写作铁律

1. **无源不写** —— 所有实测数字逐条标注出处，查不到来源的数字不写入。
2. **冲突并列** —— 同一指标多个数值全部并列呈现，不换算、不加权、不取单值。
3. **争议不裁** —— 学术争议只列双方论据，不作裁决。
4. **一篇一地** —— 一个页面即一个具体地点，标题用地名。
5. **不虚构来源** —— 引用的链接必须来自实际检索；发布前抽查源文复核数字。

## 本地预览

```bash
cd docs && python -m http.server 8124
# 打开 http://127.0.0.1:8124/
```

## 重新生成与自检

```bash
python build_site.py     # site_data.py（数据） → docs/index.html（单页标签式渲染）
python validate.py       # 标签闭合、内链、纯散文约束、卡片结构、关键内容；结果写 _validate.txt
python diag_prose.py     # 措辞诊断：标签词重复、破折号叠用、字段缺失；结果写 _diag_prose.txt
```

`site_data.py` 是纯数据（`REGION` / `AGENTS` / `CASES` / `CONFUSIONS` / `TIMELINE`），
`build_site.py` 是纯渲染。改内容只动 `site_data.py`。

构建脚本不删除 `docs/` 目录，只就地覆盖并按清单清理多余文件——本机沙箱会把 `shutil.rmtree`
改写成回收站操作并失败，不要改回去。

## 发布状态（GitHub Pages）

**已发布：https://wadesha.github.io/huaihai-geomorphology/**
仓库：https://github.com/Wadesha/huaihai-geomorphology （Pages 源：`main` 分支 `/docs` 目录，已含 `.nojekyll`）

> 本机出口代理拦截 git 推送（`git push` 的 receive-pack POST 被代理 502，
> 直连 443 也不通），发布改经 GitHub API（Git Data API：blobs → tree → commit → refs）写入远端，
> 文件内容与本地完全一致，但**远端与本地 git 历史不同源**。
> 在能连 GitHub 的环境里同步一次即可接续推送：
> `git fetch origin && git reset --hard origin/main`（内容一致，重置无数据损失）。

## 性质说明

本站为读书笔记性质的二次整理，内容是通用地貌学结论 + 公开来源的实测数据，
不替代任何教材或官方发布；实地情况请以现场为准。
